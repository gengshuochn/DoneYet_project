# DoneYet 产品需求文档与开发指导

## 一、项目概述

DoneYet 是一个运行在本地电脑上的个人健康记录 Web 服务，用于记录每日饮食营养摄入、健身训练情况和身体数据变化。项目优先开发电脑版网页，采用深色、极简、清晰、高效的界面风格。数据存储在电脑本地，后续手机端通过 Tailscale 访问电脑本地服务。

项目核心目标是让用户可以快速记录每日饮食、训练和身体指标，并通过顶部统计栏、图表和日历视图实时了解当天状态和长期趋势。

------

# 二、产品定位

## 2.1 产品名称

DoneYet

## 2.2 使用场景

用户每天在电脑端打开本地网页，记录：

1. 今天吃了什么，每餐的营养与热量估算。
2. 今天练了什么，训练内容、时长和消耗热量。
3. 当前身体数据，包括体重、胸围、腰围、体脂率、基础代谢等。
4. 通过日历查看每天是否完成合理饮食和训练。

## 2.3 目标用户

有健身、减脂、增肌、饮食管理需求的个人用户。

## 2.4 核心原则

1. 本地优先：数据存储在电脑本地，不依赖云服务。
2. 桌面优先：第一阶段只开发电脑版网页。
3. 深色极简：界面风格克制、低干扰、信息密度适中。
4. 快速记录：减少输入步骤，支持卡片式和模块化编辑。
5. 实时统计：输入后自动汇总营养、热量和训练消耗。
6. 可扩展：后续可支持手机端、更多身体指标、导入导出等功能。

------

# 三、整体产品结构

DoneYet 分为三个主模块，由顶部导航栏或侧边导航栏进行切换：

1. 饮食模块
2. 健身模块
3. 数据模块

页面最上方始终显示全局统计导航栏，用于展示今日关键数据。

------

# 四、全局顶部导航栏需求

## 4.1 位置

网页最上方固定显示，所有模块共用。

## 4.2 内容

顶部导航栏应展示以下信息：

1. 今天日期
   格式建议：YY/MM/DD 或 YYYY-MM-DD，例如 2026-06-30。

2. 当前基础代谢
   显示最近一次录入的基础代谢数据，例如：BMR 1680 kcal。

3. 今日摄入总热量
   汇总饮食模块中今天所有饮食条目的热量。

4. 今日三大营养元素总量
   包括：

   - 蛋白质
   - 碳水化合物
   - 脂肪

5. 今日运动消耗
   汇总健身模块中今天所有训练条目的热量消耗。

6. 今日热量盈余或缺口
   计算公式建议：

   今日热量盈余 = 今日摄入热量 - 当前基础代谢 - 今日运动消耗

   如果结果为正，显示为盈余；如果为负，显示为缺口。

## 4.3 视觉要求

1. 深色背景。
2. 信息分组清晰。
3. 使用低饱和度颜色突出关键数据。
4. 不使用花哨装饰。
5. 当前选中的模块需要有明显但克制的高亮状态。

------

# 五、模块一：饮食模块

## 5.1 模块目标

饮食模块用于记录每日饮食条目，例如早餐、午饭正餐、晚饭、加餐等。每个饮食条目以卡片形式展示，卡片内包含条目名称、条目内容和条目统计。

## 5.2 页面结构

饮食模块页面包含：

1. 日期范围：默认展示今天的数据。
2. 新增饮食条目按钮。
3. 饮食条目卡片列表。
4. 每张卡片内部包含：
   - 条目名称
   - 条目内容模块
   - 条目营养统计

## 5.3 新增饮食条目

用户点击“新增条目”按钮后，系统创建一张新的饮食卡片。

默认名称可以为：

- 新饮食条目
- 早餐
- 午饭正餐
- 晚饭
- 加餐

第一版可以简单使用“新饮食条目”，用户可自行修改。

## 5.4 饮食条目卡片字段

每个一级饮食条目包含以下字段：

1. id
   系统生成的唯一标识。
2. date
   所属日期。
3. title
   条目名称，例如“午饭正餐”。
4. items
   饮食内容数组。
5. createdAt
   创建时间。
6. updatedAt
   更新时间。

## 5.5 条目名称编辑

用户可以直接编辑饮食卡片标题。

交互建议：

1. 点击标题进入编辑状态。
2. 输入完成后按 Enter 或失焦保存。
3. 标题为空时恢复默认名称或提示不能为空。

## 5.6 条目内容模块化记录

每个饮食卡片内部显示已登记的饮食内容模块，以及一个加号按钮。

已登记内容以小模块形式展示，例如：

- 鸡胸肉 150g
- 米饭 200g
- 鸡蛋 2 个

每个内容模块建议显示：

1. 食物名称
2. 数量或重量
3. 热量
4. 蛋白质
5. 碳水
6. 脂肪

## 5.7 添加饮食内容

用户点击卡片内的加号按钮后，打开添加内容表单或弹窗。

需要输入：

1. 食物名称
2. 数量或备注
3. 热量 kcal
4. 蛋白质 g
5. 碳水 g
6. 脂肪 g

第一版不需要接入食物数据库，所有数据由用户手动输入。

## 5.8 编辑和删除饮食内容

每个饮食内容模块应支持：

1. 编辑
2. 删除

编辑成功后，卡片统计和顶部导航栏数据应自动更新。

## 5.9 饮食条目统计

每张饮食卡片底部应统计该餐的营养数据：

1. 总热量
2. 总蛋白质
3. 总碳水
4. 总脂肪

统计结果由该卡片内所有内容模块自动求和。

## 5.10 饮食模块整体统计

饮食模块所有当天卡片的数据需要汇总到顶部导航栏。

------

# 六、模块二：健身模块

## 6.1 模块目标

健身模块用于记录每天的训练内容。结构与饮食模块类似，采用一级训练卡片 + 二级训练内容模块的形式。

## 6.2 页面结构

健身模块页面包含：

1. 日期范围：默认展示今天的数据。
2. 新增训练条目按钮。
3. 训练条目卡片列表。

## 6.3 新增训练条目

用户点击“新增训练”后，系统创建一张训练卡片。

默认名称可以为：

- 新训练
- 胸肩训练
- 背部训练
- 腿部训练
- 有氧训练

第一版可以使用“新训练”，由用户自行修改。

## 6.4 训练条目卡片字段

每个一级训练条目包含：

1. id
2. date
3. title
   训练标题，例如“胸肩训练”。
4. items
   训练内容数组。
5. durationMinutes
   本次训练时长，单位分钟。
6. estimatedCalories
   本次训练热量消耗估算，单位 kcal。
7. createdAt
8. updatedAt

## 6.5 训练标题编辑

交互与饮食条目标题编辑一致：

1. 点击标题进入编辑状态。
2. 输入完成后按 Enter 或失焦保存。
3. 标题不能为空。

## 6.6 训练内容模块化记录

训练卡片内部显示已记录的训练内容模块，以及一个加号按钮。

训练内容模块示例：

- 卧推：60kg × 8 × 4
- 哑铃飞鸟：12kg × 12 × 4
- 跑步机：30 分钟

每个训练内容模块建议包含：

1. 动作名称
2. 重量
3. 次数
4. 组数
5. 备注

不同训练类型字段可能不完全一致，第一版可以采用通用字段：

1. name
   动作名称。
2. detail
   训练详情文本，例如“60kg × 8 × 4”。
3. note
   备注，可选。

## 6.7 添加训练内容

用户点击训练卡片内的加号按钮后，打开添加训练内容表单或弹窗。

需要输入：

1. 动作名称
2. 训练详情
3. 备注，可选

## 6.8 训练时长和热量消耗

每张训练卡片应支持记录：

1. 训练时长，单位分钟。
2. 热量消耗估算，单位 kcal。

热量消耗第一版由用户手动输入，不需要自动计算。

## 6.9 编辑和删除训练内容

每个训练内容模块应支持：

1. 编辑
2. 删除

编辑成功后，顶部导航栏中的今日运动消耗应自动更新。

## 6.10 健身模块整体统计

当天所有训练卡片的 estimatedCalories 字段求和，作为今日运动消耗显示在顶部导航栏。

------

# 七、模块三：数据模块

## 7.1 模块目标

数据模块用于记录身体指标，展示身体数据趋势，并通过日历视图查看每日完成情况。

## 7.2 数据模块页面结构

数据模块包含以下区域：

1. 身体数据记录区
2. 身体数据折线图区
3. 体脂率和基础代谢记录区
4. 月度日历视图区

------

## 7.3 身体数据记录

身体数据支持记录以下指标：

1. 体重
2. 胸围
3. 腰围
4. 当前体脂
5. 当前基础代谢

其中体重、胸围、腰围需要根据已有记录实时渲染折线图。

## 7.4 添加身体数据按钮

在身体数据图表旁边放置“添加数据”按钮。

点击后打开表单或弹窗。

## 7.5 添加身体数据表单

表单字段：

1. 日期
   用户可以选择两位年份、两位月份、两位日期。
   示例：26 / 06 / 30。
   系统内部建议转换为标准日期格式：2026-06-30。
2. 体重
   单位 kg。
3. 胸围
   单位 cm。
4. 腰围
   单位 cm。
5. 体脂率
   单位 %，可选。
6. 基础代谢
   单位 kcal，可选。

第一版可以允许所有字段手动输入。

## 7.6 身体数据字段

每条身体数据记录包含：

1. id
2. date
3. weight
4. chest
5. waist
6. bodyFat
7. bmr
8. createdAt
9. updatedAt

## 7.7 折线图需求

需要分别渲染以下三项指标的折线图：

1. 体重趋势
2. 胸围趋势
3. 腰围趋势

图表要求：

1. 横轴为记录时间。
2. 纵轴为对应数据。
3. 按日期升序展示。
4. 数据更新后实时刷新。
5. 深色背景适配。
6. 不需要复杂动画。
7. 图表旁边或上方有“添加数据”按钮。

## 7.8 当前体脂和基础代谢

数据模块应显示最近一次录入的：

1. 当前体脂率
2. 当前基础代谢

基础代谢同时同步到顶部导航栏。

------

# 八、日历视图需求

## 8.1 日历目标

日历视图用于按月查看每日健康状态，包括当天热量盈余和完成情况。

## 8.2 展示范围

以月度为单位展示。

用户可以切换：

1. 上个月
2. 下个月
3. 回到当前月

## 8.3 每个日期格展示内容

每个日期格下方显示：

1. 日期数字
2. 当天热量盈余或缺口
3. 当天完成状态图标

## 8.4 热量盈余计算

每日热量盈余计算公式：

每日热量盈余 = 当日饮食摄入总热量 - 当日基础代谢 - 当日训练消耗热量

如果某天没有录入基础代谢，使用最近一次有效基础代谢记录。

## 8.5 完成状态判断

每一天的完成状态分为三类：

### 绿色对号图标

条件：

1. 当天完成合理饮食。
2. 当天完成训练。

### 蓝色图标

条件：

1. 当天只完成合理饮食或只完成训练。

### 红色禁止图标

条件：

1. 当天既没有完成合理饮食，也没有完成训练。

## 8.6 合理饮食判断规则

第一版建议使用简单规则：

如果当天有至少一条饮食记录，则视为完成饮食记录。

后续可以扩展为：

1. 热量盈余在合理范围内。
2. 蛋白质达到目标。
3. 脂肪和碳水在目标范围内。

## 8.7 完成训练判断规则

第一版建议使用简单规则：

如果当天有至少一条训练记录，且训练消耗热量大于 0 或训练内容不为空，则视为完成训练。

------

# 九、视觉设计要求

## 9.1 整体风格

1. 深色模式。
2. 极简。
3. 高级感。
4. 少边框，多留白。
5. 卡片式布局。
6. 低饱和色彩。
7. 不使用复杂渐变和过度动画。

## 9.2 推荐颜色

可以使用以下色彩方向：

1. 页面背景：#0B0D10
2. 卡片背景：#15181D
3. 二级模块背景：#1E2229
4. 主要文字：#EDEFF2
5. 次要文字：#8B929E
6. 边框颜色：#2A2F38
7. 强调色：#7C8CFF
8. 成功绿色：#42D392
9. 提示蓝色：#4DA3FF
10. 警示红色：#FF5C5C

## 9.3 字体和布局

1. 字体使用系统默认无衬线字体。
2. 主体最大宽度建议 1200px 到 1440px。
3. 页面左右留白适中。
4. 卡片圆角建议 12px 到 18px。
5. 输入框、按钮和卡片保持统一圆角。
6. 所有数字统计应清晰可读。

------

# 十、数据存储需求

## 10.1 存储方式

第一版使用电脑本地存储。可以选择以下任一方式：

推荐方案：

1. 后端使用 SQLite 数据库。
2. 前端通过本地 API 读写数据。
3. SQLite 文件保存在项目目录下的 data 文件夹中。

不建议第一版只使用浏览器 localStorage，因为后续手机端通过 Tailscale 访问时，需要多个设备访问同一个电脑本地服务。

## 10.2 数据实体

建议数据模型包括：

1. meals
   饮食一级条目。
2. meal_items
   饮食内容模块。
3. workouts
   健身一级条目。
4. workout_items
   健身内容模块。
5. body_records
   身体数据记录。

------

# 十一、建议数据库结构

## 11.1 meals 表

字段：

1. id
2. date
3. title
4. created_at
5. updated_at

## 11.2 meal_items 表

字段：

1. id
2. meal_id
3. name
4. amount
5. calories
6. protein
7. carbs
8. fat
9. created_at
10. updated_at

## 11.3 workouts 表

字段：

1. id
2. date
3. title
4. duration_minutes
5. estimated_calories
6. created_at
7. updated_at

## 11.4 workout_items 表

字段：

1. id
2. workout_id
3. name
4. detail
5. note
6. created_at
7. updated_at

## 11.5 body_records 表

字段：

1. id
2. date
3. weight
4. chest
5. waist
6. body_fat
7. bmr
8. created_at
9. updated_at

------

# 十二、API 设计建议

## 12.1 饮食 API

### 获取某日饮食记录

GET /api/meals?date=2026-06-30

### 新增饮食条目

POST /api/meals

请求体：

```json
{
  "date": "2026-06-30",
  "title": "午饭正餐"
}
```

### 更新饮食条目

PATCH /api/meals/

请求体：

```json
{
  "title": "午饭正餐"
}
```

### 删除饮食条目

DELETE /api/meals/

### 新增饮食内容

POST /api/meals//items

请求体：

```json
{
  "name": "鸡胸肉",
  "amount": "150g",
  "calories": 240,
  "protein": 36,
  "carbs": 0,
  "fat": 6
}
```

### 更新饮食内容

PATCH /api/meal-items/

### 删除饮食内容

DELETE /api/meal-items/

------

## 12.2 健身 API

### 获取某日训练记录

GET /api/workouts?date=2026-06-30

### 新增训练条目

POST /api/workouts

请求体：

```json
{
  "date": "2026-06-30",
  "title": "胸肩训练"
}
```

### 更新训练条目

PATCH /api/workouts/

请求体：

```json
{
  "title": "胸肩训练",
  "durationMinutes": 75,
  "estimatedCalories": 480
}
```

### 删除训练条目

DELETE /api/workouts/

### 新增训练内容

POST /api/workouts//items

请求体：

```json
{
  "name": "卧推",
  "detail": "60kg × 8 × 4",
  "note": "最后一组力竭"
}
```

### 更新训练内容

PATCH /api/workout-items/

### 删除训练内容

DELETE /api/workout-items/

------

## 12.3 身体数据 API

### 获取身体数据记录

GET /api/body-records

### 新增身体数据

POST /api/body-records

请求体：

```json
{
  "date": "2026-06-30",
  "weight": 72.5,
  "chest": 98,
  "waist": 82,
  "bodyFat": 16.5,
  "bmr": 1680
}
```

### 更新身体数据

PATCH /api/body-records/

### 删除身体数据

DELETE /api/body-records/

------

## 12.4 统计 API

### 获取某日顶部统计

GET /api/summary/daily?date=2026-06-30

返回示例：

```json
{
  "date": "2026-06-30",
  "bmr": 1680,
  "intakeCalories": 2300,
  "protein": 145,
  "carbs": 260,
  "fat": 70,
  "exerciseCalories": 480,
  "calorieBalance": 140
}
```

### 获取某月日历数据

GET /api/calendar?year=2026&month=6

返回示例：

```json
[
  {
    "date": "2026-06-01",
    "calorieBalance": -320,
    "hasDiet": true,
    "hasWorkout": true,
    "status": "complete"
  },
  {
    "date": "2026-06-02",
    "calorieBalance": 120,
    "hasDiet": true,
    "hasWorkout": false,
    "status": "partial"
  }
]
```

------

# 十三、前端开发要求

## 13.1 技术栈建议

推荐使用：

1. React
2. TypeScript
3. Vite
4. CSS Modules 或普通 CSS
5. Recharts 或 ECharts 用于折线图
6. Zustand 或 React Context 管理前端状态

如果希望简单快速，可以使用：

1. React + TypeScript + Vite
2. Tailwind CSS
3. Recharts

## 13.2 页面结构建议

前端目录建议：

```text
src/
  components/
    AppShell.tsx
    TopStatsBar.tsx
    NavigationTabs.tsx
    EditableTitle.tsx
    Card.tsx
    Modal.tsx
    StatPill.tsx

  modules/
    diet/
      DietPage.tsx
      MealCard.tsx
      MealItemChip.tsx
      MealItemForm.tsx

    workout/
      WorkoutPage.tsx
      WorkoutCard.tsx
      WorkoutItemChip.tsx
      WorkoutItemForm.tsx

    body/
      BodyDataPage.tsx
      BodyRecordForm.tsx
      BodyTrendChart.tsx
      HealthCalendar.tsx

  api/
    client.ts
    meals.ts
    workouts.ts
    bodyRecords.ts
    summary.ts

  utils/
    date.ts
    nutrition.ts
    calendar.ts

  types/
    index.ts
```

## 13.3 主要交互

1. 顶部导航切换三个模块。
2. 饮食模块可以新增、编辑、删除饮食卡片。
3. 饮食卡片内可以新增、编辑、删除饮食内容模块。
4. 健身模块可以新增、编辑、删除训练卡片。
5. 训练卡片内可以新增、编辑、删除训练内容模块。
6. 数据模块可以新增身体数据。
7. 新增或编辑任何数据后，顶部统计栏和相关图表自动刷新。
8. 日历视图可以切换月份。

------

# 十四、后端开发要求

## 14.1 技术栈建议

推荐使用以下任一方案：

方案 A：

1. Node.js
2. Express 或 Fastify
3. SQLite
4. better-sqlite3
5. TypeScript

方案 B：

1. Python
2. FastAPI
3. SQLite
4. SQLAlchemy 或 sqlite3

考虑到 Codex 开发和前后端统一，建议使用方案 A：Node.js + Express + SQLite + TypeScript。

## 14.2 后端目录建议

```text
server/
  src/
    index.ts
    db/
      connection.ts
      schema.ts
      migrations.ts

    routes/
      meals.ts
      workouts.ts
      bodyRecords.ts
      summary.ts
      calendar.ts

    services/
      mealService.ts
      workoutService.ts
      bodyService.ts
      summaryService.ts
      calendarService.ts

    utils/
      date.ts
```

## 14.3 本地启动

建议开发启动方式：

```bash
npm run dev
```

生产启动方式：

```bash
npm run build
npm run start
```

本地服务默认端口建议：

```text
http://localhost:3000
```

或前后端分离：

```text
前端：http://localhost:5173
后端：http://localhost:3001
```

## 14.4 Tailscale 访问预留

第一版先保证电脑本机可用。

后续手机访问时，需要让后端监听：

```text
0.0.0.0
```

而不是只监听：

```text
127.0.0.1
```

并在局域网或 Tailscale 网络中通过电脑的 Tailscale IP 访问。

------

# 十五、数据计算规则

## 15.1 单餐营养统计

单餐总热量 = 该 meal 下所有 meal_items.calories 求和。

单餐蛋白质 = 所有 meal_items.protein 求和。

单餐碳水 = 所有 meal_items.carbs 求和。

单餐脂肪 = 所有 meal_items.fat 求和。

## 15.2 今日饮食统计

今日摄入总热量 = 今天所有 meal_items.calories 求和。

今日蛋白质 = 今天所有 meal_items.protein 求和。

今日碳水 = 今天所有 meal_items.carbs 求和。

今日脂肪 = 今天所有 meal_items.fat 求和。

## 15.3 今日运动消耗

今日运动消耗 = 今天所有 workouts.estimated_calories 求和。

## 15.4 当前基础代谢

当前基础代谢 = body_records 中 date 小于或等于今天，且 bmr 不为空的最近一条记录。

## 15.5 今日热量盈余

今日热量盈余 = 今日摄入总热量 - 当前基础代谢 - 今日运动消耗。

------

# 十六、MVP 范围

第一版必须实现：

1. 深色极简桌面端界面。
2. 顶部导航栏和全局统计。
3. 饮食模块 CRUD。
4. 饮食内容模块 CRUD。
5. 饮食营养自动汇总。
6. 健身模块 CRUD。
7. 健身内容模块 CRUD。
8. 训练时长和热量消耗记录。
9. 身体数据新增。
10. 体重、胸围、腰围折线图。
11. 当前体脂和基础代谢展示。
12. 月度日历视图。
13. 每日热量盈余展示。
14. 每日状态图标展示。
15. SQLite 本地持久化存储。

第一版暂不实现：

1. 用户登录。
2. 云同步。
3. 手机端独立 UI。
4. 食物数据库搜索。
5. 自动识别食物热量。
6. 复杂训练热量计算。
7. 多用户系统。
8. 数据导入导出。
9. 目标热量和目标营养设置。

------

# 十七、Codex 开发指导

## 17.1 开发目标

请开发一个名为 DoneYet 的本地 Web 应用。该应用用于记录饮食、健身和身体数据。第一阶段只开发桌面端网页，要求深色、极简、本地 SQLite 存储。

## 17.2 推荐实现方式

请使用以下技术栈：

1. 前端：React + TypeScript + Vite
2. 后端：Node.js + Express + TypeScript
3. 数据库：SQLite，推荐 better-sqlite3
4. 图表：Recharts
5. 样式：CSS Modules、普通 CSS 或 Tailwind CSS 均可，但必须保证深色极简风格

## 17.3 项目结构

请使用单仓库结构：

```text
doneyet/
  package.json
  README.md
  client/
  server/
  data/
```

client 存放前端代码。
server 存放后端代码。
data 存放 SQLite 数据库文件。

## 17.4 实现优先级

请按以下顺序开发：

第一步：搭建项目结构。
第二步：实现 SQLite 数据库表结构。
第三步：实现后端 API。
第四步：实现顶部统计栏。
第五步：实现饮食模块。
第六步：实现健身模块。
第七步：实现身体数据模块和折线图。
第八步：实现月度日历视图。
第九步：统一深色极简视觉样式。
第十步：补充 README 启动说明。

## 17.5 前端页面要求

前端只有一个主页面，但内部通过导航切换三个模块。

顶部固定显示：

1. 当前日期
2. 当前基础代谢
3. 今日摄入热量
4. 今日蛋白质
5. 今日碳水
6. 今日脂肪
7. 今日运动消耗
8. 今日热量盈余

导航选项：

1. 饮食
2. 健身
3. 数据

## 17.6 饮食模块开发要求

请实现：

1. 新增饮食卡片。
2. 编辑饮食卡片标题。
3. 删除饮食卡片。
4. 在饮食卡片内新增饮食内容。
5. 编辑饮食内容。
6. 删除饮食内容。
7. 自动统计每张饮食卡片的热量、蛋白质、碳水、脂肪。
8. 自动更新顶部今日饮食统计。

饮食内容字段：

```ts
type MealItem = {
  id: string;
  mealId: string;
  name: string;
  amount: string;
  calories: number;
  protein: number;
  carbs: number;
  fat: number;
};
```

## 17.7 健身模块开发要求

请实现：

1. 新增训练卡片。
2. 编辑训练标题。
3. 删除训练卡片。
4. 在训练卡片内新增训练内容。
5. 编辑训练内容。
6. 删除训练内容。
7. 每张训练卡片可以记录训练时长。
8. 每张训练卡片可以记录热量消耗估算。
9. 自动更新顶部今日运动消耗。

训练内容字段：

```ts
type WorkoutItem = {
  id: string;
  workoutId: string;
  name: string;
  detail: string;
  note?: string;
};
```

## 17.8 数据模块开发要求

请实现：

1. 新增身体数据。
2. 展示最近一次体脂率。
3. 展示最近一次基础代谢。
4. 使用折线图展示体重变化。
5. 使用折线图展示胸围变化。
6. 使用折线图展示腰围变化。
7. 支持月度日历视图。
8. 每个日期显示当天热量盈余。
9. 每个日期根据饮食和训练完成情况显示状态图标。

身体数据字段：

```ts
type BodyRecord = {
  id: string;
  date: string;
  weight?: number;
  chest?: number;
  waist?: number;
  bodyFat?: number;
  bmr?: number;
};
```

## 17.9 日历状态规则

请按以下规则实现：

```ts
if (hasDiet && hasWorkout) {
  status = "complete"; // 绿色对号
} else if (hasDiet || hasWorkout) {
  status = "partial"; // 蓝色图标
} else {
  status = "missed"; // 红色禁止图标
}
```

图标可以使用文本或 SVG：

1. complete：绿色 ✓
2. partial：蓝色 • 或 ◐
3. missed：红色 ⛔ 或 ×

## 17.10 样式要求

请保持整体界面风格：

1. 深色背景。
2. 极简设计。
3. 卡片式布局。
4. 清晰数字统计。
5. 不要使用复杂动画。
6. 不要使用明亮大面积色块。
7. 输入框、按钮、卡片风格统一。
8. 页面在 1200px 到 1440px 宽度下体验良好。

## 17.11 错误处理要求

请处理以下情况：

1. 数字输入为空时按 0 处理或提示用户。
2. 标题为空时不允许保存。
3. 日期格式错误时提示用户。
4. 删除数据前可以使用浏览器 confirm 确认。
5. API 请求失败时在页面显示简单错误提示。

## 17.12 README 要求

请在 README 中写明：

1. 项目介绍。
2. 技术栈。
3. 安装依赖方式。
4. 启动前端方式。
5. 启动后端方式。
6. SQLite 数据库存储位置。
7. 后续如何通过 Tailscale 访问。

## 17.13 验收标准

项目完成后应满足：

1. 打开网页后可以看到深色极简界面。
2. 可以在饮食模块新增一餐，并添加多个食物。
3. 食物营养数据会被自动汇总。
4. 顶部栏会显示今日总摄入。
5. 可以在健身模块新增一次训练，并添加多个训练动作。
6. 训练热量消耗会同步到顶部栏。
7. 可以在数据模块新增身体数据。
8. 体重、胸围、腰围可以显示为折线图。
9. 日历可以显示每一天的热量盈余。
10. 日历可以根据饮食和训练情况显示绿色、蓝色或红色状态。
11. 刷新页面后，数据仍然存在。
12. 数据存储在本地 SQLite 文件中。

